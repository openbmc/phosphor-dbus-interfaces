description: >
    Implement this to add Hardware dump management.

    Hardware dump collects hardware states including
    various register values from each processor during
    a system checkstop.
