description: >
    Implement to provide sensor rated values.

    All Sensor.RatedValue properties are read-only.

properties:
    - name: MaxValue
      type: double
      default: infinity
      description: >
          The Maximum rated value of sensor.
    - name: MinValue
      type: double
      default: -infinity
      description: >
          The Minimum rated value of sensor.
