description: >
    This interface allows to reset persistent metric counters. Applicable to
    metrics such as RebootCount, RebootWithFailureCount.

methods:
    - name: Reset
      description: >
          Resets the presistent count for the metric.
      errors:
          - xyz.openbmc_project.Common.Error.InternalFailure
