description: >
    Implement to provide the module ECC attributes.

properties:
    - name: CECount
      type: size
      description: >
          A correctable ECC event has been detected on a device/module.
      default: 0
    - name: CELimit
      type: size
      description: >
          The limit number of the correctable ECC event.
    - name: UECount
      type: size
      description: >
          An uncorrectable ECC event has been detected on a device/module.
      default: 0
    - name: UELimit
      type: size
      description: >
          The limit number of the uncorrectable ECC event.

associations:
    - name: contained_by
      description: >
          ErrStatistic may implement an 'inventory' to 'ErrStatistic'
          association with the inventory item containing to it.
      reverse_name:
          - containing
      required_endpoint_interfaces:
          - xyz.openbmc_project.Inventory.Item
