description: >
    Implement on an entity manager configuration that describes a platform,
    meaning configuration that spans several entities and so cannot be attached
    to any one of them. It carries no properties of its own; the configuration
    is described by the interfaces alongside it.
